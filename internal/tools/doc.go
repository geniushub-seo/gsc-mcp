// Package tools implements the MCP tool handlers for Google Search Console.
// Each handler converts LLM-friendly inputs into GSC API calls, normalizes
// site URLs, applies request timeouts, and returns structured results with
// explicit error bodies.
package tools
